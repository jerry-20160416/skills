---
name: dingtalk-kb-download
description: 下载钉钉知识库文件 - 递归下载所有嵌套文件夹，保持完整文件夹结构。触发词：钉钉下载、知识库下载、dingtalk download
version: "1.0.0"
license: MIT
metadata:
  hermes:
    tags: [dingtalk, download, knowledge-base, 钉钉, 知识库]
---

# 钉钉知识库下载技能

递归下载钉钉知识库文件，保持完整文件夹结构。

## 触发场景

- 用户想下载钉钉知识库
- 用户想备份钉钉文档
- 用户提到"钉钉下载"、"知识库下载"、"dingtalk download"

## 资源文件

| 文件 | 说明 |
|------|------|
| `scripts/dingtalk-download-structured.py` | 主下载脚本 |
| `scripts/钉钉知识库下载工具操作手册.md` | 详细使用文档 |

---

## 前置条件

### 1. 环境检查

执行下载前，确认以下环境：

```bash
# 检查 Python 版本（需要 ≥ 3.10）
python --version

# 检查 Playwright 是否安装
pip show playwright
```

### 2. 安装依赖（如果未安装）

```bash
pip install playwright
playwright install chromium
```

---

## 执行步骤

### 步骤 1：确认下载目录

询问用户下载目录，默认：`D:\钉钉知识库备份`

### 步骤 2：修改输出目录（如需要）

编辑脚本最后一行的 `output_dir` 参数：

```python
# scripts/dingtalk-download-structured.py 第 344 行
downloader = DingTalkStructuredDownloader(output_dir="D:\\钉钉知识库备份")
```

### 步骤 3：启动下载脚本

```bash
python scripts/dingtalk-download-structured.py
```

### 步骤 4：指导用户操作

脚本启动后，输出以下指引：

```
============================================================
📖 钉钉知识库下载工具已启动
============================================================

请按以下步骤操作：

1. 📱 在打开的浏览器中扫码登录钉钉
   - 使用钉钉 App 扫码
   - 或使用账号密码登录

2. 📂 导航到要下载的知识库文件夹
   - 点击进入目标文件夹
   - 工具会自动开始递归下载

3. ⏸️ 需要停止时按 Ctrl+C

============================================================
💡 提示：
   - 自动递归下载所有子文件夹
   - 已存在的文件会自动跳过（增量下载）
   - 保持原有文件夹结构
============================================================
```

### 步骤 5：监控下载进度

脚本会自动显示进度：

```
📁 [1级] 文件夹名/
  找到 25 个项目
  [1/20] 📄 文件名.docx
     ✓ 已保存: 文件名.docx
  ...
```

### 步骤 6：下载完成

显示统计信息：

```
============================================================
📥 下载完成统计
============================================================
   处理文件夹: 3 个
   文件总数: 35 个
   下载成功: 35
   下载失败: 0
   跳过(已存在): 0
============================================================

📁 文件保存在: D:\钉钉知识库备份
```

---

## 配置参数

### 修改输出目录

编辑脚本第 344 行：

```python
downloader = DingTalkStructuredDownloader(output_dir="D:\\你的目标目录")
```

### 修改下载间隔

| 参数 | 位置 | 默认值 |
|------|------|--------|
| 文件下载间隔 | 第 89 行 | 0.5 秒 |
| 子文件夹间隔 | 第 93 行 | 1 秒 |
| 下载超时 | 第 105 行 | 60 秒 |

---

## 常见问题处理

### 问题 1：浏览器未打开

**原因**：Playwright 浏览器驱动未安装

**解决方案**：

```bash
playwright install chromium
```

### 问题 2：登录超时

**原因**：180 秒内未完成扫码登录

**解决方案**：重新运行脚本，尽快扫码

### 问题 3：下载中断

**原因**：网络问题或手动中断

**解决方案**：

```bash
# 重新运行脚本，已下载文件会自动跳过
python dingtalk-download-structured.py
```

### 问题 4：文件名乱码

**原因**：文件名包含特殊字符

**解决方案**：脚本已自动处理，将特殊字符替换为下划线

---

## 注意事项

| 事项 | 说明 |
|------|------|
| 浏览器窗口 | 保持打开，关闭会停止下载 |
| 大文件夹 | 可能需要较长时间 |
| 增量下载 | 已存在文件自动跳过 |
| 文件夹结构 | 完整保持原有结构 |

---

## 相关文件

| 文件 | 说明 |
|------|------|
| `scripts/dingtalk-download-structured.py` | 主下载脚本 |
| `scripts/钉钉知识库下载工具操作手册.md` | 详细使用文档 |

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0.0 | 2026-06 | 初始版本，支持递归下载 |

---

**维护者**: jerry.xh